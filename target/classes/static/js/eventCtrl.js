bulkAppControllers.controller("eventCtrl", function($scope, $http) {
	/*
		if (!$rootScope.userId) {
			$location.path("/");
		}
		var eventId = $routeParams.eventId;
		var postData = $.param({
			json: JSON.stringify({
				userId: userId,
				eventId: eventId
			})
		});
		$http.post("/eventData", postData).success(function(data, status) {
			$scope.events = data.entries;
		});
	*/

});